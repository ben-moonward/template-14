/**
 * allow both create and update
 * - transform on both ends? could be too much work/redundant
 * handle dynamic array fields
 * handle dynamic toggle fields
 */

// need to define a common form building pattern
// query -> transform -> initialstate
// where should type truth go? at the end? in the formik object?
// both beginning and end sounds like a royal pain to transform twice

// transformPayloadToCreate ?
// transformPayloadToForm & transformFormToCreate ?
